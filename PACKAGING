deps:
-qt 4.4, with sqlite plugin
-kdelibs 4.2, with kross (recommend krosspython, python-dbus)
-kdesdk strigi analyzer plugins (for project tree view)
-recommend gettext (it is used as a fallback for getting plural expressions for langs, and by kross scripts)
